#include <stdio.h>
long int f(long int n)
{
  if (n==1)
    return 1;
  else
    return n*f(n-1);
}

int main(void) {

  for(long int i=1;i<=20;i++)
    {
      printf("\n Factorial of %ld : %ld ",i,f(i));
    }

  return 0;
}
